export default class Event {

    constructor(name, status, action, abbreviation){
        this.name = name
        this.status = status
        this.action = action
        this.abbreviation = abbreviation
    }

}
	