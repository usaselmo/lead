export default class Filter {

    constructor(pageRange, lines, searchText, event){
        this.pageRange = pageRange
        this.lines = lines
        this.searchText = searchText
        this.event = event
    }

}